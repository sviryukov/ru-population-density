import {Map} from "./components/Map";

const App = () => {
    return (
        <div style={{width: '1280px', height: '720px', margin: 'auto'}}>
            <Map/>
        </div>
    );
};

export default App;